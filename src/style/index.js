import { makeStyles } from "@mui/styles";

const useStyle = makeStyles({
  content: {
    backgroundColor: "red",
    color: "#fff",
  },

  title: {
    fontSize: 50,
  },
});

export default useStyle;
